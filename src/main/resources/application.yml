server:
  port: 8880
spring:
  redis:
    host: 127.0.0.1
    port: 6379
    database: 0
    pool:
      max-active: 8
      max-wait: -1
      max-idle: 8
      min-idle: 6
    timeout: 3
  datasource:
    username: root
    password: password
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/jsjyz_v?unicode=true&characterSet=utf-8&TimeZone=UTC
#  sql:
#    init:
#      schema-locations: classpath*:sql/schema/*.sql
#      data-locations: classpath*:sql/data/*.sql
  mvc:
    static-path-pattern: /static/**
mybatis-plus:
  configuration:
    map-underscore-to-camel-case: true
  mapper-locations: classpath*:mapper/*.xml
